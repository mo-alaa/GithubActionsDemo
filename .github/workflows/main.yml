name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: 'echo "Hello World!"'
      - name: try new command
        run: echo ${{ github.ref_name }}  
      - name: create env var
        run: export MY_VAR=$([ github.ref_name == "main" ] && echo "Hello from main" || echo "Hello from dev")
      - name: echo var
        run: echo $MY_VAR
      # - name: Extract branch name
      #   shell: bash
      #   run: echo "BRANCH_NAME=${GITHUB_HEAD_REF:-${GITHUB_REF#refs/heads/}}" >> $GITHUB_OUTPUT
      #   id: extract_branch
      # - name: use branch name
      #   run: echo ${{ env.BRANCH_NAME }}
      
